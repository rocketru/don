<%= form_with(model: author_post) do |form| %>
  <% if author_post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(author_post.errors.count, "error") %> prohibited this author_post from being saved:</h2>

      <ul>
        <% author_post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :author_id, style: "display: block" %>
    <%= form.text_field :author_id %>
  </div>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
