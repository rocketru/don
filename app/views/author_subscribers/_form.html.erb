<%= form_with(model: author_subscriber) do |form| %>
  <% if author_subscriber.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(author_subscriber.errors.count, "error") %> prohibited this author_subscriber from being saved:</h2>

      <ul>
        <% author_subscriber.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :author_id, style: "display: block" %>
    <%= form.text_field :author_id %>
  </div>

  <div>
    <%= form.label :subscriber_id, style: "display: block" %>
    <%= form.text_field :subscriber_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
